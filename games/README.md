# Сохранённые игры

Эта папка содержит старые экспортированные файлы игр. При первом запуске сервер переносит совместимые CSV в SQLite.

Новые игры сохраняются в `../data/mafia-host.sqlite3`, поэтому отдельный CSV для каждой игры больше не создаётся.
